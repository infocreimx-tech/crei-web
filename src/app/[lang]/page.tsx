// The approved redesigned experience is now the main localized homepage.
// Keeping it as a shared component also preserves /[lang]/experiencia as a comparison URL.
export { default } from "./experiencia/page";
